import { useState } from "react"

function Slider(){
    const image= [
    
    ]
            const [current ,setcurrent] = useState(0)

            const nextSlide = ()=>{
                setcurrent(current===Image.length)
            }

    return(
        <>

        </>
    )
}

export default Slider